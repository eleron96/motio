# Dummy values for unit tests (vitest). Tests mock all network calls;
# these only satisfy client construction at import time. Safe to commit.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
